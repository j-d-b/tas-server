/// <reference types="express" />
/// <reference types="cors" />
/// <reference types="body-parser" />
import * as express from 'express';
import * as corsMiddleware from 'cors';
import { OptionsJson } from 'body-parser';
import { ApolloServerBase } from 'apollo-server-core';
export interface ServerRegistration {
    app: express.Application;
    server: ApolloServerBase<express.Request>;
    path?: string;
    cors?: corsMiddleware.CorsOptions;
    bodyParserConfig?: OptionsJson;
}
export declare const registerServer: ({ app, server, path, cors, bodyParserConfig, }: ServerRegistration) => Promise<void>;
